config = {}

config['tipfy'] = {
    'extensions': [
        # Enable debugger. It will be loaded only in development.
        'tipfy.ext.debugger',
    ],
}

config['tipfy'] = {
    'apps_installed': [
         'apps.users',
     ],
}
