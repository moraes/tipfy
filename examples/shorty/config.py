config = {}

config['tipfy'] = {
    'extensions': [
        # Enable debugger. It will be loaded only in development.
        'tipfy.ext.debugger',
    ],
    'apps_installed': [
        'apps.shorty',
    ]
}
