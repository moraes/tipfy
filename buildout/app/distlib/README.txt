Warning!
========

This directory is removed every time the build tool runs, so don't place or
edit things here because any changes will be lost!

Use the "/lib" directory to place other libraries or to override packages
from this directory.